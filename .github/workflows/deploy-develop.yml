name: deploy-develop

on:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 8.0.x

    - name: Update appsettings.json
      run: echo '${{ secrets.DEVELOP_APPSETTINGS }}' > Line/appsettings.json

    - name: Publish with dotnet
      run: dotnet publish --configuration Release Line/Line.csproj

    - name: Copy files
      run: cp -r Line/bin/Release/net8.0/publish containers/line/bin

    - name: AWS CLI install
      run: |
        curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
        unzip awscliv2.zip
        sudo ./aws/install --update
        aws --version

    - name: AWS set Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ secrets.AWS_REGION }}

    - name: Deploy
      run: |
        aws ecr get-login-password --region ${{ secrets.AWS_REGION }} | docker login --username AWS --password-stdin ${{ secrets.AWS_REPOSITORY_ID }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com
        cd containers/line
        docker build -t nagiyu-line .
        docker tag nagiyu-line:latest ${{ secrets.AWS_REPOSITORY_ID }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/nagiyu-line:latest
        docker push ${{ secrets.AWS_REPOSITORY_ID }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/nagiyu-line:latest

    - name: Update Lambda Function
      run: |
        aws lambda update-function-code --function-name dev-nagiyu-line --image-uri ${{ secrets.AWS_REPOSITORY_ID }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/nagiyu-line:latest

    # - name: Remove Nginx Settings in target path
    #   uses: appleboy/ssh-action@master
    #   with:
    #     host: ${{ secrets.SSH_HOST }}
    #     username: ${{ secrets.SSH_USERNAME }}
    #     key: ${{ secrets.SSH_PRIVATE_KEY }}
    #     port: ${{ secrets.SSH_PORT }}
    #     script: |
    #       sudo rm -rf /etc/nginx/sites-enabled/line
    #       sudo rm -rf /etc/nginx/sites-available/line
    #       sudo rm -rf ${{ secrets.DEVELOP_TARGET_PATH }}/nginx

    # - name: Upload Nginx Settings to server
    #   uses: appleboy/scp-action@master
    #   with:
    #     host: ${{ secrets.SSH_HOST }}
    #     username: ${{ secrets.SSH_USERNAME }}
    #     key: ${{ secrets.SSH_PRIVATE_KEY }}
    #     port: ${{ secrets.SSH_PORT }}
    #     source: nginx/*
    #     target: ${{ secrets.DEVELOP_TARGET_PATH }}

    # - name: Replace nginx configuration and Reload nginx
    #   uses: appleboy/ssh-action@master
    #   with:
    #     host: ${{ secrets.SSH_HOST }}
    #     username: ${{ secrets.SSH_USERNAME }}
    #     key: ${{ secrets.SSH_PRIVATE_KEY }}
    #     port: ${{ secrets.SSH_PORT }}
    #     script: |
    #       sudo cp ${{ secrets.DEVELOP_TARGET_PATH }}/nginx/line /etc/nginx/sites-available/
    #       sudo ln -s /etc/nginx/sites-available/line /etc/nginx/sites-enabled/
    #       sudo systemctl restart nginx

    # - name: Remove all files in target path
    #   uses: appleboy/ssh-action@master
    #   with:
    #     host: ${{ secrets.SSH_HOST }}
    #     username: ${{ secrets.SSH_USERNAME }}
    #     key: ${{ secrets.SSH_PRIVATE_KEY }}
    #     port: ${{ secrets.SSH_PORT }}
    #     script: |
    #       cd ${{ secrets.DEVELOP_TARGET_PATH }}/containers
    #       docker compose -f docker-compose.yml -p develop_line down
    #       docker volume prune -f && docker image prune -f && docker builder prune -f
    #       sudo rm -Rf *

    # - name: Upload artifact to server
    #   uses: appleboy/scp-action@master
    #   with:
    #     host: ${{ secrets.SSH_HOST }}
    #     username: ${{ secrets.SSH_USERNAME }}
    #     key: ${{ secrets.SSH_PRIVATE_KEY }}
    #     port: ${{ secrets.SSH_PORT }}
    #     source: containers/*
    #     target: ${{ secrets.DEVELOP_TARGET_PATH }}

    # - name: Recreate Line
    #   uses: appleboy/ssh-action@master
    #   with:
    #     host: ${{ secrets.SSH_HOST }}
    #     username: ${{ secrets.SSH_USERNAME }}
    #     key: ${{ secrets.SSH_PRIVATE_KEY }}
    #     port: ${{ secrets.SSH_PORT }}
    #     script: |
    #       cd ${{ secrets.DEVELOP_TARGET_PATH }}/containers
    #       docker compose -f docker-compose.yml build --no-cache
    #       docker compose -f docker-compose.yml -p develop_line up -d
